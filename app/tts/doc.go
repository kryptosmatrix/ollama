// Package tts is the desktop-app speech stack: ElevenLabs synthesis, secret
// storage, optional encrypted cache, and the types the UI HTTP layer serves.
//
// It is imported by app/ui. It is not imported by the inference server. There
// is no TTS route on ollama serve.
package tts
