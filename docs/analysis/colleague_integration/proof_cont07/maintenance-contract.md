# Continuation 07 — bounded diagnostic and type maintenance

Authority: Ash's current request to continue and standing pre-approval in the conversation and DECISIONS.md. Owner: Eko. Worktree: /Users/krypto/GitHub/ollama-eko-chat-read-integrity. Baseline HEAD: 281dcaa06c7cd8b299c6045a248dd6b6ac85f954. This is existing-code maintenance, not a new feature blueprint or earned programme-completion verdict.

## Native declaration slice

N-01: Remove only the three whitespace separators between operator\"\" and _cls/_sel/_str in app/webview/webview.h. Preserve function bodies, types and consumers.
N-02: Compile the actual webview.cc translation unit with the existing C++11/Cocoa/static configuration and -Werror=deprecated-literal-operator. The original source fails for exactly these three diagnostics; the corrected source must pass without suppression.
N-03: Under the same ordinary -O2 object compilation, require identical emitted object bytes before/after this syntax-only edit. Retain both objects and command outputs. Run the real app/cmd/app test target for the composed native build; do not claim an application launch.
N-04: Retain an isolated original-declaration control. Recheck the clean source after the control. An arbitrary build error does not satisfy the diagnostic control.

## Frontend type slice

F-01: Replace explicit any annotations in StreamingMarkdownContent.tsx and MessageList.tsx with existing component/domain types or inference. Preserve all runtime statements, values, branches, rendering and IO. Do not change the generated types or ESLint policy.
F-02: Type the existing custom-citation AST node in remarkCitationParser.ts through mdast's registry augmentation so the existing node discriminant narrows correctly. Preserve both parsing passes, payload fields and duplicate-removal behaviour; remove only the now-unnecessary casts/directives.
F-03: Require identical JavaScript emission for each changed existing TypeScript source, with identical compiler options and comments removed. This is a compatibility property for a type-only repair, not a claim of new runtime functionality.
F-04: Use the existing TypeScript checker, ESLint and full frontend test runner. Add direct real-renderer/parser regression coverage where the current tests substitute Streamdown. Test contrasting content, citation selection and code rendering without mocked internal components. Keep baseline violations visible; no ignore rules, blanket suppressions, test skips or weakened assertions.

## Proof and safety

Record every attempted command, actual child status, output hashes, source identities and any narrow failed attempt before repair. Obtain independent DeepSeek review with fresh bounded execution before calling either slice accepted. Defer any new runtime semantics to a separate contract rather than disguise them as lint fixes.

The history-preservation mechanism and original checkout are not edited. The Keychain tests are not retried, modified through an alternate route or disabled. No toolchain/global configuration change, existing-installation replacement, main merge or irreversible deletion is included. The duplicate -lobjc flag is generated by the installed Go build tool; do not silence it or weaken dependency linkage to obtain a green log.
