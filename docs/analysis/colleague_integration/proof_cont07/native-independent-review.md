# Independent Native Declaration Review — N-01…N-04

**Scope.** Only N-01–N-04. Diagnostic/semantic-compatibility repair, not a GUI feature. No Keychain, GUI or frontend tests; no full integration/release verdict.

## Source anchors
- Header `app/webview/webview.h` (sha256 `60b93d50…eef0`), lines 1589/1592/1595: `operator""_cls`, `operator""_sel`, `operator""_str` — no space between `""` and the suffix. Bodies (1590, 1593, 1596) and consumers unchanged.
- Contract N-01–N-04 (`maintenance-contract.md`, sha256 `ca4b3516…c2aa`), baseline `281dcaa06c7cd8b299c6045a248dd6b6ac85f954`.
- Reconciliation (`precode-reconciliation.md`, sha256 `5467b2f5…5760`): pinned invocation, exactly three removed spaces, no flag/suppression change, object equality measured not presumed.
- Controller `review_native.py` (sha256 `1649ecaf…c101`): baseline header fetched via `git show`, three `operator"" `→`operator""` replacements asserted population==1, `exact_diff` computed, control run in a temp tree, `verify_native` callable once.

## Fresh checks (this run, not prior results)
- **N-01 exactness:** `three_spaces_only: true` — candidate header equals baseline with exactly the three separators removed; no other delta.
- **N-02 diagnostic control:** original source, `-Werror=deprecated-literal-operator -fsyntax-only`, exit 1 with **exactly 3** `preceded by whitespace in a literal operator declaration is deprecated` errors at 1589/1592/1595. Corrected source: exit 0, empty stderr (candidate-syntax and candidate-final). No suppression.
- **N-03 object compatibility:** `-O2 -c` original vs candidate, same flags, only output path differs → `fresh_object_bytes_identical: true`, both exit 0. (Original object emits 3 warnings, not errors, without `-Werror` — expected.)
- **N-04 app assembly:** `go test -count=1 -json ./app/cmd/app` exit 0; all four tests `pass`, `unfinished: []`: `TestManagerExistsWithAnEmptyConfiguration`, `TestAServerAddedAfterLaunchCanBeConnected`, `TestManagerIsRefusedWhenMCPIsSwitchedOff`, `TestManagerConnectsWhatIsAlreadyConfigured`. No launch claimed.

## Disclosed, not suppressed
`remaining_warnings`: `ld: warning: ignoring duplicate libraries: '-lobjc'` — generated by the installed Go build tool; left visible per contract line 23.

## Limits
- Object-byte equality is a measured result for this toolchain/flag set only; it is not a semantic proof beyond this syntax-only edit.
- App-assembly tests exercise the composed native build, not a running application.
- Frontend slice (F-01–F-04) was blocked by the platform; not run, not routed around, not judged here.
- No Keychain/GUI execution; no programme acceptance.

All four N-items are satisfied by fresh, independently executed evidence.

NATIVE_DECLARATION_REVIEW=PASS