# Decisions and scope record

Owner: Eko. Date: 20 September 2026, Australia/Brisbane.
These are commissioning and checkpoint decisions, not implementation-blueprint promotion or release approval.

| ID | Category | Decision | Basis and boundary |
|---|---|---|---|
| commission-eko-controller | DECISION | Eko takes the implementation-controller role for the saved work list. | Ash's current request explicitly commissions the work. Eko remains separate from the recurring Ollama colleague to be founded under OI-06. |
| retain-thirty-one-items | DECISION | Track all 31 OI identifiers, not just the six newest features. | The complete R3 work list is authoritative; `REQUIREMENTS.md` is only a navigation/status index. |
| establish-baseline-before-code | DECISION | Continue OI-01 before promoting design or editing runtime code. | Current TECHNE and the work list require source reconciliation and actual proof. A commissioning document is not an implementation-ready blueprint. |
| preserve-existing-paths | DECISION | Treat the existing terminal and desktop request paths as separate recon targets. | Source anchors are in `SOURCE_MAP.md`. This does not yet select persistence ownership, an API, a migration or a context algorithm. |
| preserve-command-evidence | DECISION | Do not erase old Bridge records or alter service settings to unblock execution without the relevant approval. | Two command-admission failures are retained verbatim. Earlier unrelated logs and idempotency records are not disposable task scratch space. |
| new-checkpoint-only | DECISION | Create only new programme records under the work list's proposed `docs/analysis/colleague_integration/` location. | The directory was created explicitly. Existing tracked source and the two existing untracked documents are not edited or staged. The records are local-only while command execution is blocked. |
| no-self-issued-review | DECISION | Leave independent review and readiness verdicts unissued. | No reviewer was invoked. Eko changing roles inside this session does not satisfy independence. |
| explicit-parallel-lane | RECONCILIATION | Treat the Ollama work as Ash's separately commissioned lane, not a replacement of the research programme. | The live PSYCHE North Star records ANAMNESIS isolation-first as the current objective. No NEXUS runtime reintegration or estate-wide repair is started here. |

## Material decisions not yet made

Settings schema and migration, colleague identity storage/binding, LETHE scope and outbox contracts, context-budget allocation and fallback, export snapshot semantics, child authority enforcement, scheduler persistence and interruption semantics, model qualification thresholds, release packaging and rollback remain subject to the actual source requirements and their design/review gates.

Do not convert a source observation or this checkpoint into approval of any of those contracts. Do not erase a requirement merely because an existing implementation cannot yet satisfy it.

## 20 September 2026 — authorised recovery and resumed baseline

The initial decisions above remain historical. Ash's “Please do” explicitly authorised preservation-first archive maintenance. The executed scope was one fingerprint-verified harmless diagnostic plus 32 backed-up terminal jobs, retired through the existing service operation. Preserve exact job tombstones; do not change the service limits or deploy the unqualified newer Bridge candidate. Strongest objection: forgetting public job records without preserving their hidden specifications would destroy evidence. The bootstrap fingerprint check and private full-row/input/output backup address that objection within the recorded scope. Alternatives rejected: blind deletion, direct database mutation, deleting tombstones, and installing a newer unqualified service. Evidence: BRIDGE_MAINTENANCE_2026-09-20.md and its linked receipts.

Two existing request tests were selected for a first bounded baseline because their model client is a fixture and no live model or app launch is required. They passed; this does not promote helper consistency into end-to-end proof. Broader baseline and independent review remain owed. The companion cross-model handoff is read but does not change R3's authority or adopt optional direct child messaging.

## Continuation 02 — 20 September 2026

- Ash explicitly selected Ollama `deepseek-v4.1-flash:cloud` as the adversarial judge. Only selected task source and synthetic evidence were disclosed through the existing wrapper; two bounded calls, one complete source-only FAIL report.
- Diagnose and repair the reproduced continuation/history-loss prerequisite before building automatic memory on that path. Keep the six proposed requirements and the two-file error-classification boundary; do not silently add GET/rename UX repairs.
- Use Tier 2 for this HTTP-observable contract. Neither the author's draft nor a source-only model response earns implementation readiness. Preserve original reviewed bytes and account for the complete finite findings list.
- Keep baseline lint failures and the full-root Keychain-test isolation gap visible. No blanket warning suppression or destructive test-isolation shortcut.
- Preserve all task command outputs and move only task-generated binaries into the verified Git-ignored build directory, without deleting bytes or rewriting historical execution records.

## Standing authority — 20 September 2026

Decision ID: `ash-eko-standing-preapproval-2026-09-20`.
Source: Ash's direct message in the active Ollama implementation conversation, after Eko requested the bounded maintenance exception. This records the user's instruction; it is not a self-issued permission or a reviewer verdict.

> Eko, I need you to remember something. I trust you to make the changes that you have reasoned are necessary. I draw the line at deleting unrecoverable things from my computer, that is all. So if you have decided something needs to happen, you have my full pre-approval to do so, as long as you both tell me you're going to do it, and have reasoned that it is required.

Operational application: reason about necessity and scope, tell Ash what will change and why before acting, then proceed without another routine approval question. Keep an actual recovery path for changes that could lose unique data. A file being untracked, apparently redundant, or called temporary does not establish recoverability. Protect against unrecoverable loss through overwrites and migrations as well as deletion commands; when recovery cannot be established, leave the irreplaceable material untouched. This delegation does not turn unavailable tools, platform restrictions or unperformed verification into permission or evidence.

Immediate decision: apply the proposed maintenance exception in `DECISION_REQUEST_CHAT_READ_REPAIR.md` to the two-function chat-history preservation correction. The reason is the reproduced load-error-to-history-replacement defect; this is a necessary prerequisite to reliable conversation memory. Preserve all six requirements R-01 through R-06 and the independent pre-code design review, permanent tests, authenticated HTTP, persistence, deliberate-break, regression and independent executable-review obligations. Ordinary Method 16 promotion and its dependent Method 03 readiness gate are not required for this bounded repair; no earned `[IMPLEMENTATION-READY]` stamp is claimed. No general TECHNE rewrite or wider proof exemption is made.

This decision resolves `chat-continuation-integrity-route-authority` and supersedes the earlier pending-authority statements in the decision request, HANDOFF, STATE and BLOCKERS. Historical failed reviews and test results remain historical and unchanged. Review access and implementation/acceptance remain separate technical obligations, not reasons to ask Ash for the same approval again. DeepSeek `deepseek-v4.1-flash:cloud` remains the selected primary adversarial judge.

Persistence boundary: this instruction is retained in the repository's decision log and referenced by its resumption records. No automatic cross-conversation ChatGPT memory update is asserted.


## Continuation 06 — 20 September 2026

- Correct the actual source-hiding ignore rule, not only force-add its existing victims. Preserve coverage-artifact and credential-file exclusions; use actual Git checks and old-rule controls. Independent DeepSeek re-execution approved that bounded correction.
- Run the complete default app package suite after reading its side-effect surfaces, with fresh filesystem paths and the existing TTS test's uniquely named synthetic Keychain service. This does not make HOME a Keychain sandbox. The resulting macOS authorisation cancellation is a failed test, not a pass or a reason to bypass consent. No automated retry was made.
- Compare exact lint findings and affected source bytes against main, not totals alone. Both matches are true; both lint processes still fail. Generated code needs a durable generator-side remedy, not an unreviewed manual edit or hidden suppression.
- The Bridge refused the proposed root MCP Keychain fixture correction as recognised credential content. Preserve the original file and refusal record; do not route the same mutation through a different tool.
- Preserve the ignore fix and curated proof on the existing authorised remote branch, retain the worktree, and leave main/installation unchanged while the actual integration gate is blocked. Historical passing mechanism evidence stays scoped; historical failures remain recorded.


## 21 September 2026 — continuation 07

Ash's standing pre-approval was applied after announcing the necessary changes: three native literal-operator spaces and bounded frontend type repairs. The custom-theme type owner was explicitly added after tsc exposed the existing mismatch hidden by any. No runtime statement, generated type, lint policy, theme value or dependency changed. Independent native execution passed; frontend source review passed, but runtime tests were platform-blocked and not rerouted. Eleven written cases remain unexecuted.

Preserve both review rounds and the static counterexamples that refuted incorrect reviewer claims. Do not weaken assertions or add casts to appease a disproven objection. Keep the earlier Keychain denial and tool-denied fixture edit intact, and do not change global Go/linker settings to silence -lobjc. Native commit abeb2e5e29d838f7252b271f25c3b40d1bdff41c; frontend wip acd8bb3de93ea01d43dd8783bbeff036113f9ab9; remote documentation checkpoint d559da25a4db89fa9369d8be4106100803e82a58. This is preservation, not integration or programme completion.

## 21 September 2026 — continuation 08 authority and test-environment correction

Ash: “Thank you Eko, if you have work remaining, please continue that work with full authority from myself.” Existing recoverability and explain-before-action boundaries remain. The renewed direction authorised the continuation; it did not waive testing, platform controls or macOS consent.

The admitted normal npm execution exposed a CSS collection defect and an incorrect new test expectation. Eko repaired the default Vitest configuration and matched the test to the installed Streamdown markup without changing production rendering. All eleven new cases and the full 154-case suite passed, three compiled faults were detected, and DeepSeek independently repeated the required runs. FRONTEND_RUNTIME_REVIEW=PASS is bounded to that scope, not integration or release. No Keychain attempt or generated-code/lint-rule suppression was made.

## 21 September 2026 — continuation 09

Ash supplied the Eko Compass continuation prompt and requested resumption. Its standing authority is the current commission; no new blanket approval is required. Keep generator lint work finite and separate from product delivery. Existing Go metadata owns public JSON types; a guarded annotation adapter may not infer public contracts or become a second runtime implementation. The reviewed R3 contract and all three source-review attempts are retained in continuation-09 evidence. Formal promotion and independent executable acceptance are not claimed.

Eko's first Program-comparison success statement was premature and wrong; the actual TS5096 failure, retraction and corrected successful comparison are retained. An outer shell exit is not a child-test result. The later consumer-test input was refused before execution and was not rerouted. Therefore production implementation was not started; only the explicitly unaccepted test candidate and design/evidence were committed and backed up. No Keychain, installation, main integration, dependency or lint-policy change occurred.

## Continuation 10 — generated-model gate closed; return to product dependencies

Source 6a377818 satisfies the bounded G-01–G-07 maintenance contract after self-proof, eleven fresh selected-DeepSeek executions, and completion of missing source/raw-log inspection. Public JSON types remain Go-owned; runtime constructors and emitted JavaScript are preserved. No dependency/lint-policy change, interface substitution or generated-file hand edit was used. The original judge reports remain unchanged; source-backed corrections live in evidence/2026-09-21_cont10/REVIEW_RECONCILIATION.md.

The failed ^node Go selector, recoverable line-zero read handling, reporting-loop exhaustion and filename-only read-coverage gap were instrument/review failures, not production defects. Failed attempts were retained, the same review history resumed, limits remained bounded, and missing bodies/logs were supplied before Eko accepted the bounded verdict. Exact source/command evidence and dispositions are in the continuation-10 directory. Normalised lint findings are equal; raw stdout-byte equality is not claimed.

This is the generator slice's finite exit, not permission to expand cleanup indefinitely. Return to OI-01's remaining capability/installed-source reconciliation and OI-02 G1 design. Keep the 18 handwritten errors, 11 warnings, build/dependency/Keychain/root/package obligations visible at their applicable gates; do not invent a dependency requiring the whole repository clean before memory design. All 31 programme items remain unaccepted. Source accepted for maintenance does not mean merged or installed.

## Continuation 11 — G1 instruction design, not runtime delivery

Eko applied existing standing authority to the next product slice rather than more general cleanup. The source-grounded concept and SPEC-DRAFT blueprint establish a shared instruction owner, immutable revisions, a pinned per-conversation selection, explicit idle reload and both real request adapters. Full context admission remains unresolved; the measured cloud num_ctx and zero-output counterexamples are retained, and a one-token measurement is not mislabelled free tokenization. DeepSeek performed a limited draft review; all nine findings have dispositions, not an invented implementation pass. The new public-handler diagnostic timed out without retained partial output; this author instrument failure and the separate blocked read/inspection are explicit. No user data, main, installation or permanent test suite changed. See CONTINUATION_11_REPORT.md and its source/probe records.

## Continuation12 — recover once; reuse the actual admission boundary

Authority: Ash's explicit request to investigate the failed turn and resume, with the attached Compass and standing pre-approval unchanged. Fresh Git/remote/command evidence, not the screenshot, established what survived.

The auxiliary one-token-inference budget proposal is rejected. Pinned source provides a non-inferencing count endpoint for preview and an existing actual-token/selected-slot guard before prefill. The future output allowance belongs alongside that guard; exact output/ingress/backend policy is still assigned design work. This selection replaces the unnecessarily early pre-queue concept, not a production implementation. All18 frozen G1 requirements and the31-item programme scope remain unchanged; no provider/backend obligation is silently deleted.

The new entry baselines are complete but red for the absent feature. Reference digest checks cover null bindings only. Three DeepSeek consultation messages are one continued source review, not independent execution or a promotion panel. The author's version-probe output-capture error and closeout parser mismatch are preserved in continuation12 evidence; no test assertion or raw result was rewritten. Native timeout/refusal is not bypassed and does not establish what the binary internally did.
