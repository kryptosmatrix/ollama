# Continuation 10 — frozen generated-model maintenance proof

Source candidate: `6a3778184d43b23c5cba14e0379922377f615f52`. The containing commit is documentation-only; its execution-surface equality and remote disposition are recorded at the primary control path `docs/analysis/colleague_integration/evidence/2026-09-21_cont10/git-closeout.json`. It is not main integration or installation.

Start with ACCEPTANCE_EVIDENCE.md, EXECUTION_INDEX.json and REVIEW_RECONCILIATION.md. The final bounded independent judgement consists of independent-review-final plus independent-review-addendum. The earlier attempts remain retained. Full default frontend: 164 passes; dedicated generation: 22; Go store: 42 including 17 top-level. All eleven reviewer-requested runs completed. The three compiled faults were detected and clean reruns passed. Full lint still fails with 18 handwritten errors and 11 warnings, with zero generated findings.

The complete programme inventory and canonical R3 commission are under checkpoint/. They accept none of OI-01–OI-31. G1_RECONNAISSANCE.md is a separate read-only follow-on, unreviewed for implementation; do not mistake it for a ready blueprint or delivered instruction persistence.

CURATION_MANIFEST.json maps original absolute paths to byte-identical raw copies or lossless JSON envelopes, with exact hashes. Files ending in .evidence.json contain the original UTF-8 text in content; its decoded SHA-256 is the original file identity. See LOSSLESS_ENCODING_RECEIPT.json. Original historical receipts are not rewritten. Candidate-bound archived scripts retain their original absolute paths and refuse a different source HEAD. They are records of the tested instruments, not a promise that moving this folder makes them run without re-establishing the declared environment. The ordinary repository generation/test commands are documented in app/ui/app/codegen/README.md. A changed candidate needs new proof; documentation-only reuse needs an unchanged execution-surface receipt.

Excluded files remain locally in primary custody. In particular, private arrival/LINEAGE text is not published; one earlier build stdout that the Bridge declined to read is not rerouted or claimed backed up here. The later independent build's full output and status are present. No user conversation database, model credential or font is included. Existing primary untracked evidence, unrelated worktrees and installed application are untouched.

The checkpoint/ control copies are a dated snapshot from before this documentation commit, not a competing control system. Live programme control records remain at the primary repository's docs/analysis/colleague_integration/. Exact final remote tip and terminal session state are recorded there after verification.
