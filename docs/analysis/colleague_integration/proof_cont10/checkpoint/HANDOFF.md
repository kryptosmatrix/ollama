# Ollama continuation 10 — generated-model maintenance closed

Owner: Eko. Source `6a3778184d43b23c5cba14e0379922377f615f52`, branch `eko/chat-read-integrity-20260920`, owned worktree `/Users/krypto/GitHub/ollama-eko-chat-read-integrity`. The source is pushed and remotely verified. Main and the installed application are unchanged. Exact documentation/remote closeout: evidence/2026-09-21_cont10/git-closeout.json.

The finite generated-type maintenance contract is satisfied: real Go-owned generation, 35 classes/16 helpers retained, identical 53,014-byte runtime JavaScript, no generated lint findings. The selected DeepSeek judge requested eleven real fresh runs; 164 frontend cases, 22 generator cases and 42 Go store cases passed, with three detected compiled faults and clean reruns. Full-body/raw-log inspection and report corrections are in its preserved addendum. Read CONTINUATION_10_REPORT.md and evidence/2026-09-21_cont10/ACCEPTANCE_EVIDENCE.md, not the historical continuation-07 source-map status.

Full lint still exits 1: 18 handwritten errors and 11 warnings. Build/dependency/duplicate-library, root Keychain isolation, supervised Keychain, combined/package and installed acceptance obligations remain. Do not bypass the recorded Keychain/fixture-edit refusals or call those gates passed.

Next safe task: finish OI-01's remaining source/installed capability map and resolve the OI-02 low-level design for G1 (saved editable instructions plus actual request delivery). No repeat approval, generator redesign, or re-derivation of completed history/renderer/native proof is needed. The whole repository need not be cleaned before memory design; retain each real integration/release gate at its own boundary. Source truth is the owned checkout; intended requirements are the canonical R3 commission.

Full OI-01–OI-31 inventory: REQUIREMENTS.md and ../../OLLAMA_COLLEAGUE_INTEGRATION_WORK_LIST.md. Programme accepted count remains zero. The bounded maintenance pass is not delivered memory or autonomy.

The current command job is 35a5b7b379654957a908c7f0d8c6825c; read session-closeout.json/STATE.json for its terminal disposition. Archived fault instruments are candidate-bound and must not silently attach old passes to new source. Documentation-only proof reuse requires execution-surface equality recorded in the Git closeout receipt. All aborted/failed attempts remain retained; report reconciliation names the corrections.

## Product-path follow-on — read-only G1 reconnaissance

See `evidence/2026-09-21_cont10/G1_RECONNAISSANCE.md`. The source has separate terminal and desktop request builders, while the complete public settings/table contract has no persisted instructions/revision/binding. Installed build metadata identifies eb8c93da rather than the tested task candidate; no installed behaviour was tested. The next bounded read is conversation/revision lifecycle and existing curated-context design, then the owning G1 low-level design. This is partial OI-01 progress, not a new feature or a design-ready verdict.
