# Generated-model maintenance — implementer self-audit

Candidate: `6a3778184d43b23c5cba14e0379922377f615f52`. This is self-review, not T-10.

## Contract and observed results

The ten consumer tests ran before production changes: nine behaviours passed;
the explicit-any check failed on 104 syntax sites. No assertion was weakened.
The committed Go delta adds only five ts_type annotations and changes the normal
generation directive; existing JSON tags and executable store/handler statements
are unchanged. The adapter never infers public field types. All 35 actual classes
and 16 conversion helpers remain. Real Program emission is identical, including a
separate comparison with the pre-metadata generated source; see
pre-metadata-emission-equivalence.json for the 53,014-byte JS hash and options.

All current valid author arms pass: ten consumer checks, 164 default frontend
regression cases, 22 dedicated generation cases including subtests, 42 Go store
cases (17 top-level), real application typecheck, frontend build, and exact lint
baseline comparison. Full lint still exits 1: 18 handwritten errors and 11
warnings. The generated target has zero findings. Build/dependency warnings are
retained, not waived. Root Keychain, supervised consent, combined/package,
main-integration and installation obligations remain outside this bounded result.

The ordinary Go generation directive was executed and returned the verified
receipt. CLI checks include actual fresh Go generation twice, transformed-byte
check success and stale refusal without writes, missing Go failure/no-write,
grammar/collision/public-type refusal, and compiler diagnostic/runtime drift
rejection. Successful inner Go output is retained in receipts; this was improved
before the frozen candidate and its later generation runs are the complete proof.

## Deliberate-break and runner evidence

The permanent tests and candidate were committed before mutations. Clean arms
bracket the three compiled faults in isolated copies: raw upstream output fails
the explicit-any assertion AND the actual ESLint rule; discarded chat messages
fail the named nested-delivery assertion; fixed message content fails contrasting
input-dependent real Markdown rendering. Additional downstream failures are
retained but are not substituted for those named AssertionErrors. Every fault
patch, hash, true child exit and full output is retained. Only exact owned
regenerable fault copies are removed; shared source is untouched.

The normal positive path uses real generated models and Markdown/remark/Shiki
components. HTTP transport is simulated explicitly; this is not browser UI,
installed-app, schema-validation or live-provider proof. Existing time/byte/raw
representation quirks are preserved rather than silently fixed.

Runner checks reject an actual child exiting 7, missing required discovery and
empty selection. The final runner extends the previous retained executor. Full
installed frontend dependency SHA-256 maps are checked, not merely the lockfile;
the serial and eight-worker implementations returned the same 36,272-entry map.
Only reproducible caches listed in frozen.json are excluded. Go module manifests
and toolchain bytes are frozen; a full Go module-cache content census is not
claimed. Final instrument versions and the equivalence measurement are retained.

## Material instrument correction — preserve the failed attempt

My first normal-entry test used `go generate -run '^node '`. Go matches the full
original directive, so this selected nothing and exited zero with no receipt.
The completion gate correctly rejected that run and prevented review launch.
Installed Go help plus wrong/correct dry runs prove the selector mismatch.
Only the test command changed to `^//go:generate node `; the exact same positive
receipt assertions then passed. No production byte, assertion, fixture expectation,
main branch or installation was changed to obtain that result. The failed record
at runs/author-11-normal-entry remains false; entry-selector-correction.json
records its disposition and the corrected run is author-13-normal-entry-corrected.
It is not an unexplained retry or a passing production result.

## Scope verdict

The current generation-maintenance requirements are supported by self-audit and
retained executions. Independent reviewer-directed reproduction is still owed.
None of OI-01–OI-31 is accepted and no memory/autonomy functionality is claimed.
Use the exact per-arm result files and raw logs, not these aggregate sentences,
to audit the result. No earlier failed receipt has been rewritten.
