Control runs of the final cloud_check2.py against fake_cloud.py (run_controls2.sh). clean.txt: six modes and the request-shape check, exit 0. sabotage/: four deliberately broken copies of the harness (START_LOST rule inverted, MIDDLE_LOST rule removed, tool-call verdict removed, think sent as null instead of omitted), each exiting 1 at its intended control (exitcodes.txt). Request and response payloads are kept for the echo mode only (the shape check reads them); other modes keep results.json, per-probe result.json, harness.log and stdout. A first harness version, written before the Codex options check, was superseded by this one before any real request; its uncommitted transcripts were discarded.
