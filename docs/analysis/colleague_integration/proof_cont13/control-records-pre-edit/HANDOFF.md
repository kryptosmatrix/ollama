# Ollama — current handoff, continuation 12

Owner Eko. Recovery verified at baedaf58a711606f343c9c83e13bb68a0169528f; source remains6a3778184d43b23c5cba14e0379922377f615f52. Read current_candidate in STATE.json for the subsequently verified closeout commit and terminal state. Owned worktree /Users/krypto/GitHub/ollama-eko-chat-read-integrity; branch eko/chat-read-integrity-20260920. Main and installed app are not this candidate.

The failed turn's two G1 design files and evidence survived in Git and on the actual remote. Platform failure cause is unknown. The prior shell is cancelled/terminal; this turn recovered the missing entry result. HTTP existing-settings control passes; missing instructions PUT returns HTML200 and fails its Profile assertion. Candidate CLI builds, ordinary --help passes, instructions --help fails unknown-command. Three null-binding reference goldens pass across Python/Go; not production persistence proof. Full details/logs: CONTINUATION_12_REPORT.md and evidence/2026-09-21_cont12/.

The design remains SPEC-DRAFT, not implemented, no formal promotion rounds, zero programme items accepted. The critical new source is native b10091 /v1/chat/completions/input_tokens plus existing actual-token/selected-slot admission at tools/server/server-context.cpp:3068–3150 (slot capacity capped at :1250–1255). Counter output is preview, not a future request receipt. Extend that guard for a finite output allowance; do not add a duplicate prompt assembler, trust Go requested NumCtx as actual capacity, or reopen the rejected auxiliary one-token inference design.

Next: specify finite output allowance/representation, versioned capability or ingress, and exact native/rendered/media/MLX/remote outcomes. Then finish G1 lifecycle/failure-number, binding-golden, CLI/test/guard closure and independent design gates. Existing opcode/message-field/runner parsing and default output policy must be read before selecting exact fields; all required current-source paths are linked in the blueprint and native-source evidence. A complete design is the prerequisite to runtime code, not another general cleanup cycle.

Native --version timed out after 20 seconds before any model-specific probe; the follow-up diagnostic was platform-refused before execution. Do not reroute it, alter macOS controls or claim the source pin qualifies the binary. Real native runtime proof remains outstanding. Preserve prior Keychain/process/fixture refusals. Unrelated source design work remains permitted under Ash's standing authority.

The 31-row REQUIREMENTS.md inventory is intact. Historical18lint errors/11 warnings, dependency/build/native-linker, root/Keychain and combined/package gates remain separate. No live user data, installed app or global protection changed; no LETHE or identity write claimed. Raw evidence is retained in the primary checkout; curated proof and exact remote/terminal closeout are in their own records.
