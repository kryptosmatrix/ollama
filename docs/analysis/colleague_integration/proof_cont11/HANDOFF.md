# Current handoff — continuation 11

Owner Eko; G1 instructions feature design is in progress, not implemented. Resume from CONTINUATION_11_REPORT.md, STATE.json and the canonical owned-worktree docs/_design/G1_PERSISTED_INSTRUCTIONS.md. The concept and eighteen G1 requirements preserve OI-03/OI-04 as a joint outcome. Neither is accepted; all 31 programme rows remain unaccepted.

The source checkpoint remains 6a377818 with incoming documentation checkpoint 17197224. Current design bytes, real Git preservation and terminal job state are in STATE.json and evidence/2026-09-21_cont11/git-closeout.json when present. Do not mistake a documentation-only commit for implemented instructions.

Executed this continuation: two existing system-prompt tests and the existing no-truncate parent/subtest; seven synthetic cloud probes. DeepSeek reviewed the draft source excerpts and retained probes, not an implementation. All nine review findings have dispositions; the revised draft has no formal readiness stamp.

Immediate remaining work: OQ-1 full context admission, OQ-3 actual restart/request/lifecycle baseline, OQ-4 derived closure/guards/test coverage and independent two-substrate promotion. The ordinary null-binding profile and inactive non-null identity boundary are specified; real identity activation belongs to G2. Read the exact section before changing it.

Do not repeat a known bad probe: cloud num_ctx=8 was not enforced for a 25-token request; num_predict=0 was rejected. One-token probes returned 25/502/502 input counts but perform real inference and do not prove general completeness or context maxima. Raw requests/responses are retained in cloud-budget-probe/.

The self-contained public-handler HTTP baseline is in evidence/2026-09-21_cont11/testdata/instructions_http_baseline_test.go. It has no usable result: the parent wrapper timed out and failed to retain partial output. A fixture read and subsequent process inspection were separately platform-blocked. No alternate access to those blocked targets occurred. Do not blindly rerun the HTTP test without safe owned-process containment and durable timeout/output capture. Inspect the job closeout record, not this paragraph, for terminal execution state.

Existing 18 lint errors/11 warnings and prior Keychain/root/package obligations remain open. They are not an excuse to restart general cleanup before resolving the finite G1 product design. Main, installed app and real conversation data are unchanged. The existing standing authority remains active; another routine approval from Ash is not required.
