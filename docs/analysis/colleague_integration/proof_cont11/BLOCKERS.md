# Current blockers — continuation 11

G1 runtime implementation is not yet authorised by an earned design/pack gate. The draft's finite open residue is full context admission (OQ-1), the actual save/restart/request and terminal lifecycle baseline (OQ-3), and complete source/guard/test coverage plus the independent Tier-2 panel (OQ-4). These are Eko's engineering tasks, not missing operator authority.

The TTS-fixture read was platform-blocked and not rerouted. A separate self-contained public-handler baseline timed out after 120 seconds; no usable test output was retained. A process-inspection request then was also platform-blocked. The dedicated owned-job control closes the command group; its actual terminal result is recorded at closeout. No new test invocation is licensed by an assumed clean process state.

DeepSeek's draft review returned changes required. Nine findings were adjudicated; several clarify the design and several were refuted. The revised document remains SPEC-DRAFT, not independently promoted or implemented. The complete source-only report and dispositions are retained.

Baseline debt is unchanged and not freshly rerun: 18 handwritten lint errors, 11 warnings; build/dependency/native-linker warnings; root Keychain isolation, supervised Keychain proof and combined/package acceptance. Only causal blockers and applicable named gates govern a given slice. Zero of 31 programme items is accepted.
