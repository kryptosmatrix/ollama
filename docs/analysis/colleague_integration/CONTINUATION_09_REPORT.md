# Continuation 09 — generated-model baseline and reviewed repair design

21 September 2026, Australia/Brisbane. Eko. **Design reviewed; implementation not started; new consumer tests unexecuted.** This is not a generator-repair pass or an accepted OI item.

## Candidate and authority

Resumed the clean owned worktree `/Users/krypto/GitHub/ollama-eko-chat-read-integrity`, branch `eko/chat-read-integrity-20260920`, at `b880138b610418b0f0cdc5922e89c8c6451c8ba9`. A fresh `git ls-remote` matched that checkpoint. Primary main stayed `953de98d408a9697fa20a48c23973b9dc8eee921`; the existing untracked programme documents and evidence were preserved.

Ash's attached Compass continuation renews the scoped standing authority and explicitly requires explanation before necessary changes, recoverability, measured controls, frozen candidates and independent review. No further blanket approval is needed. No authority was inferred to bypass platform or macOS restrictions.

The new **test-only WIP commit** is `f1d12c7820309cbda29b651a409db7850a079d1b`. It contains only `app/ui/app/src/utils/generatedModels.test.tsx` (SHA256 `86db34cc04c9dd5996f2251a47610fc218745bbde303fe3abd885b093603d4a5`). Ten cases are declared in source, not runner-discovered or executed. A later documentation checkpoint may preserve this report and proof; its identity is recorded in `evidence/2026-09-21_cont09/git-closeout.json`, rather than guessed here.

## Fresh executed baseline

Evidence root: `evidence/2026-09-21_cont09/` in the primary repository. The curated remote bundle is `proof_cont09/` in the owned task branch; it preserves exact selected bytes with a manifest.

| Actual command / boundary | Result | Evidence |
|---|---|---|
| Pinned `go run github.com/tkrajina/typescriptify-golang-structs/tscriptify` over `responses/types.go`, output to owned evidence scratch | Exit 0; raw generated bytes exactly equal committed `gotypes.gen.ts` | `baseline/regenerate.stdout`, `.stderr`, `summary.json`, `gotypes.raw.ts` |
| Actual project `tsc -b --force` | Exit 0; empty stdout/stderr | `baseline/typecheck.*`, `summary.json` |
| Actual `npm run lint -- --format=json` | Exit 1; 122 errors, 11 warnings | `baseline/lint.json`, `.stdout`, `.stderr` |
| Actual `npm test -- --run --reporter=json` BEFORE adding the new test file | Exit 0; 154/154 tests passed; no failed collections or pending tests | `baseline/tests.json`, `.stdout`, `.stderr` |

This baseline does not test the later WIP commit. No new production build, root suite, Keychain test, packaged-app launch or installation was performed. Prior bounded renderer/history/native proof remains historical evidence for its recorded source; it is not retroactively attributed to unexecuted new tests.

## The generator finding and finite design

`app/ui/ui.go:42` uses the pinned v0.2.0 CLI. Its installed source was read: `tscriptify/main.go` exposes no constructor/helper safe-typing option; `typescriptify/typescriptify.go:20–36,115,737–743` hard-codes the relevant `any` types. The output has 35 classes and 16 conversion helpers. `useChats.ts` invokes the generated `Message` and `Chat` constructors, and `src/api.ts:112–122` returns `new ChatsResponse(data)` / `new ChatResponse(data)`. Replacing classes with interfaces would remove a used runtime surface.

The selected next repair is a **named lint acceptance-gate repair**, not a claim that all memory architecture must wait on a general repository cleanup. Its finite exit is reproducible normal generation without the 104 generated explicit-any errors, preserved runtime construction, real consumer regressions, detected fault controls and independent reproduction. Other lint debt remains separate.

The independently reviewed final contract is `GENERATOR_CONTRACT.md`, including authoritative R2/R3 refinements. Exact public JSON type decisions belong in existing Go `ts_type` metadata: `ToolFunction.Result` in `app/store/store.go`; `ChatEvent.ToolResultData`, `ChatEvent.ToolState`, `ChatEvent.ApprovalArgs` and `ChatRequest.Think` in `app/ui/responses/types.go`. The proposed adapter must reject public `any`, not infer its meaning from emitted text.

The planned Node command invokes the existing pinned Go generator into scratch, applies AST-bounded constructor/helper annotations and erased assertions, checks generated-module JavaScript equivalence with the TypeScript Program API, and only then compares (`--check`) or atomically replaces the target. The two modes share one verification path. JSONValue is a fixed recursive JSON type with collision checks; it does not validate arbitrary input at runtime. No fork of the module cache, interface-only replacement, lint suppression or new dependency is proposed.

**None of that production pipeline is implemented yet.** The Go metadata tags, directive and package scripts are also unchanged. `type-probe*.mjs` and `gotypes.candidate.ts` are design experiments, not the production adapter. Their original public-field inference was superseded by Go-owned metadata in the reviewed contract. Do not install those prototypes as though they implement R3.

## Compiler experiment and Eko's corrected error

The first annotation experiment typechecked the actual application with a virtual candidate source but introduced harmless emitted parentheses. A revised experiment produced zero application diagnostics and byte-identical JavaScript under the recorded transpile options. These are compiler experiments, not actual application runs.

DeepSeek correctly challenged the difference between hand-picked transpile options and the project configuration. I then prematurely wrote a successful Program comparison into the R2 draft before reading its output. The actual first Program run exited 1 with TS5096: `allowImportingTsExtensions` is incompatible with JavaScript emission. **That success claim was wrong and was retracted in place.** The original draft, script and failure remain retained. No production source had changed and no accepted runtime result depended on that claim.

The corrected experiment parses the real `tsconfig.app.json`, records comparison-only overrides (`noEmit=false`, `allowImportingTsExtensions=false`, common comment/source-map settings), and uses `Program.emit` on the import-free generated module. It rejects diagnostics or missing output. Its actual exit is 0, with byte-identical 53,014-byte JavaScript output. Exact options and output identities are in `program-compare-r2.stdout`, `program-r2-before.js` and `program-r2-after.js`. This is not a claim about Vite's bundle, a browser, a packaged app, or safety validation. Unexpected child failures must be checked directly; an outer shell marker is not evidence of a passing child.

## Independent design review

Primary requested model: `deepseek-v4.1-flash:cloud`; returned model: `deepseek-v4.1-flash`. The local tag/manifest observation is retained in `precode-review/model.json`; it is not a model-weight immutability claim. Three source-only design consultations are retained, including both REVISE reports, dispositions and the final APPROVE in `precode-review-r3/report.md`.

Substantiated findings led to Go-owned public annotations, explicit collision/refusal rules, one shared check/write pipeline, checked Program emission, full regeneration determinism and separately named generator versus default frontend test commands. The proposed prohibition on isolated generated-file mutants was rejected: T-05 explicitly requires isolated deliberately broken copies; the no-hand-patching rule protects the accepted production output. No prior finding or failed experiment was erased.

This was **design review only**. DeepSeek did not execute code or review the new test implementation in this continuation. Independent executable acceptance remains owed after implementation.

## Blocked new test invocation

Input `eko-cont09-consumer-baseline` to Bridge job `e997c03979514b46a864eb8e69d32fb1` was refused before execution:

> This tool call was blocked by OpenAI because we couldn't determine the safety status of the request.

The requested normal command was `npm test -- --run src/utils/generatedModels.test.tsx --reporter=json`. Its child exit, discovery count and outcome are unknown because it did not run. The ten written cases cover the real `getChat` API consumer with a synthetic external transport, real generated classes and Markdown renderer, nested conversion, contrasting content, declared Date transforms, JSON payloads, optional values, map behaviour, malformed JSON and explicit-any syntax. They are not accepted tests merely because their source exists. No alternate tool, split request or direct runner was used to evade the refusal.

The existing baseline would be expected to fail the new explicit-any assertion; that expectation is source reasoning, not an executed red result. First obtain permitted execution and inspect every actual failure rather than forcing the assertions to fit. See `blocked-consumer-execution.json`.

## Preservation and remaining work

One completed task-only Bridge diagnostic record was preserved and read back before retirement to recover command capacity. Its complete 1,751-byte output was hash-verified against the Bridge read. The original request, terminal metadata and limits of captured environment information are in `retired-diagnostic-record.md`. Replay tombstones remain. No service limits, security settings, user data or source files were removed.

Main, the installed app and real conversations remain unchanged. The previous 104 generated plus 18 handwritten lint errors and 11 warnings remain; no lint reduction is claimed. Dependency/build warnings, the native duplicate-library warning, root Keychain isolation, supervised real-Keychain proof and combined/package acceptance remain open. All 31 OI items remain unaccepted.

Next action: permitted execution of the new consumer test baseline; implement the already reviewed R3 generator contract, without repeating its design debate; commit candidate and tests before isolated T-05 controls; run actual regeneration, default consumer/full frontend tests, typecheck, lint comparison, relevant Go store/UI regressions and build; obtain independent executable review. Do not bypass the recorded refusal or reopen completed history/native/renderer investigations as missing.
