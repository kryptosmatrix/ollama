Eko prematurely wrote a Program.emit success claim into the R2 design before reading the command result. The actual run exited 1 with TS5096; no production source was changed and no passing runtime result depended on it. The earlier transpileModule equality result remains a separate true measurement. Corrected the draft in place with an explicit retraction; retained R2 original, failing script and stderr. Rerun uses an explicit comparison-only allowImportingTsExtensions=false for an import-free generated module; no project compiler setting changed. Result must be read before any new claim.
